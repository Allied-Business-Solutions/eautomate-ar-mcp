# e-automate SQL Server connection settings
# Copy this file to .env and fill in your values.

EA_SERVER=absapp4
EA_DATABASE=CoAlliedBusiness

# SQL Server authentication (recommended for shared/service accounts)
# Leave both blank to fall back to Windows Authentication (Trusted_Connection).
EA_USERNAME=
EA_PASSWORD=
